import { redirect } from "next/navigation";

// Kept only for old links; TLORA Studio services now live across the main website.
export default function LegacyPlatformServicesPage() { redirect("/"); }
