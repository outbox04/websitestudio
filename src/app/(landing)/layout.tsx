import type { ReactNode } from "react";

// Registration is intentionally independent from the TLORA Studio service website.
export default function LandingLayout({ children }: { children: ReactNode }) { return children; }
